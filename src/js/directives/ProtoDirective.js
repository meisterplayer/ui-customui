
class ProtoDirective {

    constructor(meister, config) {
        this.config = config; // this is htmlUI config!
        this.meister = meister;
    }

    unload() { // eslint-disable-line class-methods-use-this

    }

}

export default ProtoDirective;
